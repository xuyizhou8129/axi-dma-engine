Look into my current rtl folder to understand my architecture, read fpga_workflow.md in docs/ folder to understand our plan of verifying it on the  
  FPGA. basically the current problem is that if I make my vitis_workplace as my top level and make it an IP block, the Microblaze will not be able    
  to directly preload values into my system memory and sram for data movement. what would be the best solution with as minimum change to my current    
  files in rtl as possible 

  vivado_config.sv 作为顶层IP wrapper，新增两个外部端口：

  module vivado_config (
      // ... 现有端口 ...

      // 新增：MicroBlaze 初始化 sys_mem 的 AXI4 slave 接口
      axi_4_if.slave  mb_sys_mem,   // 接 Vivado AXI Interconnect

      // 新增：MicroBlaze 初始化 bram 的简单写口（或AXI-Lite）
      axi_lite_if.slave mb_bram_init  // 新接口，仅写操作
  );